# CLAUDE.md — CoVet QA Brain

Handoff notes for any future Claude session working in this repo. Read this first.
Human-facing docs: `README.md` (overview), `HOW_TO_START_SERVER.txt`, `HOW_TO_DEPLOY_VERCEL.txt`.

## What this is

The QA department's internal web app ("QA Brain"), modeled on CoVet's Product Brain page.
It surfaces, from live data:
- **Roadmap** — Linear tickets labeled *Medium to Big Size Features* / *Quick wins*, grouped by
  release, each checked against Testiny for test-case coverage (has tests / missing tests).
- **Releases** (Active / Closed sub-pages) — Testiny test runs with pass/fail/blocked/skipped
  breakdown per run, including the individual failed/blocked/skipped cases (deep-linked to Testiny).
- **Bugs** and **CS Bugs** — Linear bugs grouped by release, with Active/Closed release filter and
  priority/status filters, collapsible per-release groups.
- **Manual Testing** — Testiny inventory (cases by area/type/priority).
- **Automation** — phase-2 placeholder; implement `AutomationProvider` when a CI suite exists.
- **Home** — dashboards: current effort split, release testing time (Testiny), bug cycle time by
  status/priority (Linear history, 8h workdays), and per-release median bug cycle time.
- **Access** (admin only) — approve/deny sign-ins, assign roles, create/revoke share links.

## Stack & commands

Next.js 16 (App Router, Turbopack) · React 19 · TypeScript (strict) · Tailwind v4 · Auth.js v5 (beta).
Node 24 LTS. Package manager: npm.

```
npm run dev        # dev server on http://localhost:3000
npm run build      # production build (also runs tsc)
npm run lint       # eslint (flat config, eslint.config.mjs)
npm run typecheck  # tsc --noEmit
```

Always run `npm run typecheck && npm run lint` before committing. `npm run build` is the real gate.

## Environment / hosting facts

- **GitHub**: `https://github.com/clezama-QA/Co.Vet_QA.git`, branch **`main`**.
  (Repo uses HTTPS + Git Credential Manager; there is no SSH key on the author's machine.)
- **Vercel**: project **`qahub`** (team `qa-2001`). Production URL: **https://qahub-ebon.vercel.app**.
  Auto-deploys on push to `main`.
- **Owner / sole admin**: clezama@co.vet.
- Secrets live ONLY in `.env.local` (gitignored) locally and in Vercel env vars in prod.
  **Never commit secret values.** `.env.example` lists every variable name with placeholders.
- Env var scoping gotcha: in Vercel, a variable scoped "Development" is NOT available to the hosted
  site — Production (or "All Environments") is required. All are currently on All Environments.

## Architecture map

- `src/proxy.ts` — edge auth gate (Auth.js `middleware`/proxy). Redirects unauthenticated,
  non-share-cookie traffic to `/sign-in`. Deep checks happen server-side per page.
- `src/auth.config.ts` — edge-safe Auth.js config (Google provider, domain gate, session role).
  `src/auth.ts` — full node instance adding the approval side-effects (`ensureAccessRequest`).
- `src/lib/env.ts` — the ONLY place `process.env` is read. All config/getters live here.
- `src/lib/viewer.ts` — unifies "who is looking": a signed-in **member** (with role + approval
  status) or a link **guest**. `requireAccess(href)` is the per-page server gate. `getViewer()` is
  the primitive. `allowedHrefs()` derives visible nav from `src/config/navigation.ts`.
- `src/lib/roles.ts` — role model `viewer < qa < admin`, domain check.
- `src/lib/access/` — `requests.ts` (approval records + admin email notify), `links.ts` (share
  links + which sections they unlock), `token.ts` (HMAC-signed tokens for email decision links and
  the guest cookie, signed with AUTH_SECRET).
- `src/lib/store.ts` — tiny KV: **Upstash Redis REST** in prod, `.data/store.json` file locally.
  Stores access requests (`access:<email>`) and share links (`share:<id>`).
- `src/lib/email.ts` — Resend sender; logs to console if `RESEND_API_KEY` unset.
- `src/lib/linear/` — `client.ts` (GraphQL: `fetchIssuesWithLabels`, `fetchRoadmapIssues`,
  `fetchProjectNames`), `cycle.ts` (bug cycle-time from issue history), `types.ts`, `sample-data.ts`
  (fixture used when `LINEAR_API_KEY` unset — generated by `scripts/build-linear-fixture.mjs`).
- `src/lib/testiny/` — `client.ts` (REST `/find` + `map` joins), `queries.ts` (snapshots, run
  summaries, release durations), `coverage.ts` (folder-title → ticket-id index), `types.ts`,
  `sample-data.ts`.
- `src/lib/roadmap.ts` — joins Linear roadmap tickets with Testiny coverage.
- `src/lib/bugs.ts` — Linear bugs grouped by release, active/closed derived from Testiny.
- `src/lib/release-utils.ts` — shared `RELEASE_NAME` regex + `releaseRank`/`versionRank`.
- `src/lib/worktime.ts` — 8-hour-workday math (weekends excluded) for cycle time.
- `src/content/initiatives.ts` — hand-maintained effort-allocation initiatives (Home/Automation).
- `src/components/` — small prop-driven presentational components; pages are server components.

## Integrations — how they work + hard-won gotchas

### Testiny (REST, `https://app.testiny.io/api/v1`, header `X-Api-Key`)
- Project "QA CoVet", id **1**, key **DP**. Spec: `app.testiny.io/api/v1/swagger.json`.
- Query via `POST /{entity}/find` with `{filter, map, ids, pagination, omitLargeValues}`.
- **Folder link is a mapping**, not a column: `map:{entities:["testcase","testcase_folder"]}` →
  each case gets `testcase_folder_testcase_values.testcase_folder_id`. Folder tree parent field is
  `testcase_folder_parent_id`. The magic map entity name is `testcase_folder` (underscore).
- **Run results**: `map:{entities:["testcase","testrun"]}` on `testrun/find` →
  `testrun_testcase_values[]` with `result_status`. The join FLATTENS to one row per (run,case) —
  aggregate per run id.
- **`includeDeleted:true` is REQUIRED on the run-results map join** — otherwise runs whose test
  cases were later deleted from the library return ZERO rows (this silently broke "Feature testing
  3.30"). Then skip rows with `deleted_at` (cases removed from the run) when counting.
- Case deep link: `https://app.testiny.io/{PROJECT_KEY}/testruns/tr/{runId}/tc/{caseId}`.

### Linear (GraphQL, `https://api.linear.app/graphql`, header = raw API key)
- Roadmap labels: **"Medium to Big Size Features"**, **"Quick wins"** (`QA_ROADMAP_LABELS`).
  Bug labels: **"Bug"**, **"CS Bug"** (`QA_BUG_LABEL` / `QA_CS_BUG_LABEL`). Label filter is
  case-sensitive/exact in the API.
- Roadmap excludes QA's own process tickets: labeled `qa` (`QA_ROADMAP_EXCLUDE_LABELS`) OR titled
  `^QA ` (e.g. "QA Test Design | COV-x") — these are inconsistently labeled, hence both rules.
- Releases are Linear **projects** named `"X.YZ Release"`. Bug boards seed a group for EVERY such
  project (via `fetchProjectNames`) so empty releases still show and the timeline is complete.
- Bug board scope: only release projects, "Cross-Product", or no-project bugs (see `inScope` in
  `bugs.ts`). Other backlog projects (Squad 1/2, "Bugs", Localization, Refactoring) are excluded —
  note this cut CS Bugs a lot because ~400 CS bugs live in a project literally named "Bugs".
- "Active release" = version rank >= the oldest release with an OPEN Testiny run
  (`getActiveReleaseFloor`). Non-release buckets always count as active.
- Cycle time (`cycle.ts`) uses issue `history` (state changes), counts only completed stays in
  active-pipeline statuses (backlog/unstarted/started/triage), 8h workdays via `worktime.ts`.
  Empty env label values are dangerous: `??` only catches null/undefined, so an empty string in
  Vercel overrides the code default — always set real values, never blank.

### Coverage matching (Roadmap)
A ticket "has test cases" when a Testiny folder whose TITLE mentions its id (regex `COV[-\s]?\d+`,
any case, e.g. "Cov-2230") contains cases anywhere in its subtree. See `testiny/coverage.ts`.

## Access model (see `src/lib/viewer.ts`)

1. Google SSO restricted to `ALLOWED_EMAIL_DOMAIN` (co.vet) — enforced in `auth.ts signIn`.
2. Non-admins get an access request (stored in KV) and are held at `/pending` until approved.
   Admins (`QA_ADMIN_EMAILS`) are auto-approved. Each pending sign-in emails `NOTIFY_EMAIL`
   (throttled 1/hour) with HMAC-signed one-click approve/deny links (`/api/access/decision`).
3. Roles `viewer < qa < admin`. Admin sets role per-user at approval time on `/access` (stored in
   KV, no redeploy needed). `QA_TEAM_EMAILS` is only the default role hint.
4. Share links (`/share/<id>`) set a signed guest cookie granting the exact sections the admin
   chose; revoking a link locks out existing holders immediately (re-validated every request).
   Admin pages are never shareable.

## Env vars (names only — values in .env.local / Vercel; see .env.example)

AUTH_SECRET · AUTH_GOOGLE_ID · AUTH_GOOGLE_SECRET · ALLOWED_EMAIL_DOMAIN · QA_ADMIN_EMAILS ·
QA_TEAM_EMAILS · NOTIFY_EMAIL · RESEND_API_KEY · EMAIL_FROM · APP_URL · UPSTASH_REDIS_REST_URL ·
UPSTASH_REDIS_REST_TOKEN · LINEAR_API_KEY · QA_ROADMAP_LABELS · QA_ROADMAP_EXCLUDE_LABELS ·
QA_BUG_LABEL · QA_CS_BUG_LABEL · TESTINY_API_KEY · TESTINY_PROJECT_ID.
All have code defaults except the secrets/keys. `APP_URL` must be the prod URL in Vercel
(used in email links). Changing role/label env vars in Vercel requires a redeploy to take effect.

## Conventions & environment quirks (Windows author machine)

- Shell is **PowerShell 5.1** — no `&&`/`||`/ternary; chain with `;` or `if ($?){}`. Node is at
  `C:\Program Files\nodejs`; fresh shells may need PATH refreshed:
  `$env:Path = [Environment]::GetEnvironmentVariable('Path','Machine') + ';' + [Environment]::GetEnvironmentVariable('Path','User')`.
- Multi-line/quoted git commit messages get mangled by PowerShell — write the message to a temp
  file and use `git commit -F <file>` (e.g. `.git/COMMIT_MSG_TMP`, then delete it). Use
  `git -c core.safecrlf=false commit` to avoid CRLF warnings failing the commit.
- Commit trailer used in this project's history: `Co-Authored-By: Claude ...`.
- Only commit when asked. Keep `.env.local`, `.data/`, `.claude/settings.local.json` out of git.
- Every data layer degrades gracefully: no API key → bundled sample/fixture data with a visible
  notice; API error → sample + console.warn. Never let an integration outage 500 the page.

## Known follow-ups / not done yet

- **Automation page** is a placeholder — implement `AutomationProvider` in
  `src/lib/automation/provider.ts` against real CI results (Playwright/CI artifact/pushed JSON).
- **Rotate the API keys** — Testiny, Google client secret, Resend, Upstash token, Linear key were
  all shared in chat during setup; rotate once stable and update Vercel env vars.
- **Custom email domain** (optional): verify co.vet in Resend, change `EMAIL_FROM` from
  `onboarding@resend.dev` to e.g. `QA Brain <qa@co.vet>`.
- **Deploy hygiene**: `main` auto-deploys to `qahub-ebon.vercel.app`. Verify a change in the
  browser preview when it's UI-observable (there is no automated test suite yet).
